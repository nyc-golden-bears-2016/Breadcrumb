<div>
<%if current_user == @page%>
<%=link_to 'Create Trail', new_trail_path%>
<%else%>
<%=@page.username.upcase%>'S PROFILE
<%end%>
</div>

<div>
<h5>Created Trails</h5>
<%@page.created_trails.each do |t|%>
  <div class='each-trail'>
  <%=link_to t.name, t%>
    <%if t.priv%>
      [PRIVATE]<br>
        <%if current_user == t.creator%>
          Password: <%=t.password%><br>
        <%end%>
    <%else%>
      [PUBLIC]<br>
    <%end%>
    <%if (current_user == t.creator) && !t.published%>
      <%=link_to 'Edit/Publish', "/trails/#{t.id}/edit"%><br>
      <%=form_for(t, :html => { :method => :delete }) do |f|%>
        <%= f.submit "Delete Trail" %>
        <% end %><br>
      <%elsif (current_user == t.creator) && t.published%>
        <div>*Published*</div><br>
        <%=form_for(t, :html => { :method => :delete }) do |f|%>
          <%= f.submit "Delete Trail" %>
          <% end %><br>
      <%end%>
  </div>
    <%end%>
    <% if @page.favorites %>
      <h5>Saved Trails<h5>
      <%@page.favorites.each do |t|%>
        <%=link_to t.trail.name, t.trail%>
        <%if t.trail.priv%>
          [PRIVATE]<br>
        <%else%>
          [PUBLIC]<br>
        <%end%>
        <%if current_user == @page%>
          <%=link_to "Remove from Saved", "/users/#{current_user.id}/remove/#{t.trail.id}" %>
        <%end%>
      <%end%>
    <%end%>
  </div>


  <% if @page.actives %>
    <div>
      <h5>Past Trails<h5>
      <% @page.actives.each do |t| %>
        <% trail = Trail.find(t.trail_id) %>
        <%=link_to trail.name, trail %>
        <%if trail.priv%>
          [PRIVATE]
        <%else%>
          [PUBLIC]
        <%end%>
        <%if t.completed%>
          Completed!
          <%if t.winner%>
            YOU WON!!!
          <%end%>
        <%end%>
        <%if current_user == @page %>
          <%= form_tag("/actives/#{t.id}", method: "delete") do %>
            <%= submit_tag("Delete from History") %>
          <% end %>
        <%end%>
      <%end%>
    </div>
  <%end%>

  <%if current_user == @page%>
    <div>
      <h4>Explore </h4>
        <%@trails.each do |t|%>
        <div class="each-trail-desc">
        <%=link_to t.name, t%>
          <%if t.priv%>
            [PRIVATE]
          <%else%>
            [PUBLIC]
          <%end%>
        by <%=link_to t.creator.username, t.creator%><br>
        <%=t.description%><br><br>
      </div>
        <%end%>
    </div>
  <%end%>

    <%= link_to_previous_page @trails, 'Previous' %>
    <%= paginate @trails, :window => 1, :remote => true %>
    <%= link_to_next_page @trails, 'Next' %>
</div>
